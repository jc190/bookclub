div.container
  .row
    if books
      each book in books
        .col-4.col-md-3.col-lg-2.mb-4
          img.w-100(src=book.info.thumbnail)
          a.btn-sm.btn-danger.btn-request.ml-3(href='api/user/books/delete?id=' + book._id)
            i.fa.fa-times
    else
      p No books in your library. Try adding one!
